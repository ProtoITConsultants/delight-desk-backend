services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    container_name: delight-desk-prod
    env_file:
      - .env
    environment:
      NODE_ENV: production
    ports:
      - '3000:3000'
    restart: unless-stopped
